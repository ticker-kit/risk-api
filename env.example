# Environment (dev=FakeRedis, docker=Redis container, prod=Upstash Redis)
ENV=dev

# Database connection
DATABASE_URL=postgresql://risk_user:risk_password@localhost:5432/risk_db

# JWT Authentication
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Risk Worker Service URL
RISK_WORKER_URL=http://localhost:8000

# Shared secret for risk-worker authentication
WORKER_SECRET=your-shared-secret-key-change-this

# Redis Configuration (required for ENV=prod only)
# REDIS_HOST=your-redis-host
# REDIS_PORT=6379
# REDIS_USER=your-redis-user
# REDIS_PASSWORD=your-redis-password
# REDIS_DOMAIN=rediss
# REDIS_TLS=true